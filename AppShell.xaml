<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TaskManagementApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TaskManagementApp"
    xmlns:view ="clr-namespace:TaskManagementApp.View"
    Shell.FlyoutBehavior="Disabled"
    Title="TaskManagementApp">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate view:MainPage}"
        Route="MainPage" />
    <ShellContent Title="Edit"
                  ContentTemplate="{DataTemplate view:TaskPage}"
                  />



</Shell>
